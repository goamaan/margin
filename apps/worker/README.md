# Margin Worker App

Reserved for future background workers.

Likely responsibilities include file ingestion, syllabus parsing, retrieval indexing, eval runs, import/export jobs, notification delivery, and long-running integration syncs.

Keep reusable workflow code in `packages/application`, `packages/ai`, `packages/evals`, and `packages/integrations`.
