# @margin/application

Effect application services and use cases.

Owns command/query workflows, permission checks, audit emission, policy orchestration, and ports for repositories/providers. Infrastructure packages implement those ports; apps call these services through the runtime layer.
