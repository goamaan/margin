# @margin/evals

AI and workflow evaluation harnesses.

Owns reusable eval runners, scoring helpers, regression metadata, and cost/latency capture. Concrete eval datasets can live at repo root under `evals/` when they become public-facing artifacts.
