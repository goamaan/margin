# @lantern/domain

Framework-independent LMS domain model.

Owns schemas, value objects, tagged domain errors, domain events, permission vocabulary, and invariants. It must not import from app frameworks, database adapters, AI providers, or web routes.
