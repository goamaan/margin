# @lantern/ai

AI primitives and provider boundaries.

Owns AI workflow contracts such as generation, retrieval, recommendation, evaluation, redaction, policy checks, provenance metadata, model routing interfaces, and provider adapters when they are not app-specific.

AI does not own official LMS records. Application services decide when AI output can become an official object.
